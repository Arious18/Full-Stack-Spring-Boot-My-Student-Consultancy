package tmtalyp.backend.Config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class MongoDBCleanupConfig {
    // Disabled - Spring Boot handles MongoDB cleanup automatically
}